# Trends---Server-NodeJs
The server has been hosted in Heroku platform. 
The Database used is Firebase instead of MongoDB. The server can be run locally at port 8000.
To run the server :
npm i
npm run dev
